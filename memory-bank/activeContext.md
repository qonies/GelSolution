# 当前上下文 (Active Context)

## 当前状态
**v0.8.1 已交付**（✅用户需求：参数分区折叠/展开，基线 v3.7）。
引擎 189 项 + 接口 21 项全部通过。

## v0.8.1 变更（✅用户需求：分区可折叠）
- 六个参数分区头部点击折叠/展开（▾/▸ 箭头指示；点击预设下拉不触发折叠）
- 折叠状态按分区记忆于 localStorage（键 psec_collapsed），刷新后保持
- 纯前端实现：.psec.collapsed 隐藏头部以外子元素（CSS），initCollapse()
  绑定点击（JS）；配置 JSON 结构与接口无变化
- 版本 0.8.1；基线 v3.7；测试 189+21 全部通过

## 开源许可（2026-09-09）
- ✅用户要求：设置合适的开源许可 → 选择 **MIT**（最简宽松许可：自由使用/
  修改/分发/商用仅需保留版权声明；AS-IS 免责条款契合「模拟结果仅供参考」
  定位；备选 Apache-2.0 专利授权对本项目意义不大、GPL 限制玩家传播）
- 新增 LICENSE 文件（Copyright (c) 2026 qonies）；README 新增「许可证」
  章节（中文说明 + 仅供参考/遵守当地法规/与厂商无关联三条提示）；
  纯仓库元数据变更，不改代码与版本号

## v0.8.0 变更（✅用户需求：界面参数分区与预设）
- 左侧面板重组 6 分区卡片：电机/齿轮/电池/气缸/弹簧/其他（前 5 区带预设
  下拉，其他区不设预设）；配置 JSON 结构与 CLI 完全不变，导出/导入兼容
- 预设 = configs/presets/<分区>/<名称>.json（名称=文件名，自然排序）；
  webapp 新增 GET /api/presets 下发（损坏预设降级为下拉禁用项附中文错误）
- 交互：选预设=整区载入（预设未列参数恢复系统默认）；手动修改区内任意
  输入自动转「自定义」；导入配置后全部转「自定义」
- 内置 22 套预设：电机 5（固定转速30000 + 4款曲线）/ 齿轮 3（13/16/18比1）/
  电池 2（不启用 + 3S1400-30C）/ 气缸 5（50%原装~100%）/ 弹簧 7（M75~M110）
- 顺带修复：网页 8 个器件输入缺实时重算监听（dvTappet/dvCylLen/dvPortRet/
  dvResti/dvPreload/dvPistonMass/dvDelay/dvFlag，v0.6.8/v0.7.0 新增输入漏绑定）
- 测试：新增接口 6 项（接口 21 项）+ 引擎 189 项全部通过；版本 0.8.0；基线 v3.6

## 推送前审查批次（文档同步，2026-09-09）
- ✅推送前文档/代码审查：引擎 189 项 + 接口 15 项全部通过、run.py 冒烟正常、
  版本 0.7.2 与需求基线 v3.5 一致；随后修正 README 残留旧值——基线引用
  v3.0→v3.5、引擎测试数 184→189（使用方法/目录结构两处）、气动效率
  0.21→0.243（器件基准/置信度表两处，v0.7.0 重校准遗漏）；移除已弃用的
  「最小供蛋时间ms」「供蛋最小间隔（=1000÷上限）」残留行（v0.7.1 口径）
- report.py「气缸规格」长度 %.0f→%g：72.5 mm 不再显示为 72（对齐基准值）
- git 提交身份沿用 GelSolution <dev@local>；仓库无 user.name/email 配置，
  提交时以 -c 临时指定

## 品牌引用清理批次（2026-09-09）
- ✅用户要求：全文移除第三方品牌字样（避免版权/商标纠纷）——共 14 处：
  components.py（docstring/注释×3）、report.py「气缸规格」行（→ 二号波基准）、
  网页器件参数区说明、README（基准章节标题/气缸行/置信度表×4）、
  需求基线（适用产品/硬件基准标题×2）、记忆库（projectbrief/activeContext×3）；
  统一替换为「通用二号波/二号波基准/实测」等中性表述
- report 文案变更不影响任何测试断言；引擎 189 项 + 接口 15 项全部通过

## 校准指南文档（2026-09-09）
- ✅用户要求：将实测校准事项整理成文备用 → 新增 docs/校准指南.md——
  参数置信度总览（已实测项/待校准项分优先级）、工具清单、测试方法
  （静态测量→100%缸锚点反标气动效率→多缸型隔离开孔段保留系数→
  凸轮槽角度盘法/回弹系数慢录像→供蛋/电池）、执行顺序与回归抽查、
  器件参数覆盖填写示例、注意事项、校准记录表模板
- README：「需求文档」章节改为「文档」并加指南链接；「推荐自校准流程」
  附指引；目录结构补 docs/
- 反标公式：新气动效率 = 旧值 × (实测初速 ÷ 估算初速)²；回归抽查配置
  M110/70缸/350管/7.3管7.3弹（模拟 88.3 m/s 触发警告）

## v0.7.2 最近变更（✅用户指出：前切时下一循环缩回早于拾取）
- ✅单循环时序的最后一步不能固定为「下一循环活塞拾取」：推嘴缩回起点角
  固定于扇齿 11.25°，前切 n 齿时拾取角 = n×11.25° 后移 → 前切 n≥2 时
  **下一循环推嘴缩回早于下一循环活塞拾取**
- `Timeline` 新增 `next_retract_ms`；事件表终点改为**下一循环首个事件**：
  前切 0 → 拾取（原行为）；前切 1 → 拾取=缩回重合（描述注明）；
  前切 ≥2 → 下一循环推嘴开始缩回（描述注明早于拾取 ms 数 + 回位裕量）
- 事件数保持 10；前端时序轴/事件表无需改动（排序 + 超周期红标已支持）
- 测试：新增第 22 节（5 项）；引擎 189 项 + 接口 15 项全部通过；
  版本 0.7.2；基线 v3.5

## v0.7.1 变更（供蛋判定语义澄清：能力上限=实际支持发射的能力）
- ✅澄清：弹匣/波轮的供蛋能力上限（普通 30 / 高级 60 / 压力弹匣 80 发/秒）
  指其**实际支持发射器发射水弹的能力**，直接与射速比较；**不能换算「每发
  平均供蛋时间」（1000÷上限）再与时序中的供蛋窗口比较**
- 移除判定「供蛋窗口」（判定 11 → **10 项**）；供蛋判定仅保留「供蛋速率」
  （射速 vs 能力上限，>90% 上限警告）；供蛋窗口保留为时序信息（事件表/
  指标卡片/报告照常展示，不再变色判定）
- SimConfig.feed_min_ms / min_feed_ms 删除；旧配置键 供蛋.最小供蛋时间ms
  被接受但忽略（向后兼容）；网页「最小供蛋时间」输入框移除
- 延时器结论改为定性：已安装=窗口加宽+气密代价；未安装=以实测供蛋为准
  （不再按窗口缺口定量建议）；方案枚举评分移除窗口项（3 项裕量归一化）
- 测试：新增第 21 节（6 项）+ 更新 3 处断言；引擎 184 项 + 接口 15 项
  全部通过；版本 0.7.1；基线 v3.4

## v0.7.0 变更（开孔段漏气模型更正：有漏但非全漏）
- ✅物理更正：50%~100% 缸 = 相应位置对称开 4 条横槽漏气，但活塞经过极快、
  来不及漏掉全部已压缩气体——旧模型「开孔段=完全直通大气（P=大气压、
  零压缩）」低估压力
- 新增器件参数「开孔段保留系数」port_retention（默认 **0.6**【估算】，0~1
  可覆盖，0=全漏旧模型、1=完全不漏）：开孔段按等效多变压缩 γ×保留系数；
  密封段自盖孔瞬间保留的 P、V 起算绝热压缩（弹后压力显著建立时刻提前至
  盖孔时刻）；ballistics.py 积分循环重写双分支（开孔/密封）
- 气动效率随新模型重校准 0.21 → **0.243**（保持 M90/70缸/350管 ≈71 m/s
  实测锚点，70.75 ✓）
- 后果（已入文档/测试）：默认配置初速 67.3 → **58.9 m/s**；重后切气密
  收紧——后切4 三种缸型均危险（旧「50%缸泄压段放宽」为全漏产物），
  裕量排序 100%(-1.13) < 70%(-1.05) < 50%(-0.71) 保持；50%缸+700管
  有效推力 ~340 → **475mm**（管长匹配警告场景更新为阈值机制检查）；
  强力配置 M110/70缸/350管/7.3管7.3弹 88.3 m/s 触发初速警告 ✓
- 网页器件参数区新增 开孔段保留系数（附撞击回弹系数）输入；
  报告「气缸类型」行显示保留系数；/api/default 自动预填
- 测试：新增第 20 节（10 项：锚点/初速与缸压单调性/保留1等效100%缸/
  解析/4 非法值）+ 更新 5 处受影响断言；引擎 180 项 + 接口 15 项全部通过；
  版本 0.7.0；基线 v3.3

## v0.6.9 变更（天梯齿数口径更正：默认 13.5 计 14 齿）
- ✅默认 天梯齿数 16 → **13.5（原装规格）**
- ✅半齿语义更正：半齿=第二齿磨低防咬合、**仍按整齿咬合** → 计整齿
  （13.5→**14**、11.5→**12**）：`tappet_bite()` = min(扇齿满啮合齿数, ceil(天梯齿数))
- ✅拉程基准更正：原装咬合 14 齿对应活塞满行程实测 60.5mm →
  实际拉程 = 满行程 × min(剩余咬合, 14) / 14（超过封顶，新增
  TAPPET_STOCK_BITE=14）——**默认配置行为不变**（咬合 14 ↔ 行程 60.5，
  67.3/71.3 m/s 校准锚点保持）；仅更短天梯缩短拉程
  （11.5→12 齿：行程 51.86mm、储能 2843mJ、初速 63.4 m/s、曲线模式
  电机负载转速 41313>40986 RPM；撞击剩余速度/回位稳定略降——联动方向
  已锁定入测试）
- 报告「切齿方案」行显示「天梯 X 齿计 N 齿」；网页标签同步
- 测试：重写第 19 节（17 项）+ 更新 3 处受影响旧断言（行程 10/14、
  70%缸前后各切3 压气匹配改警告 air_index=0.571、后切4 拉满 59.286）；
  引擎 169 项 + 接口 15 项全部通过；版本 0.6.9；基线 v3.2

## v0.6.8 变更（新增天梯齿数参数）
- ✅新增器件参数 `天梯齿数`（tappet_teeth，默认 16=原装；允许 13.5/11.5 等
  半齿；JSON 键「天梯齿数」，校验 4~24 拒绝非数字/布尔）
- 模型：蛋糕齿实际可咬合上限 = min(扇齿满啮合齿数, 天梯齿数)；剩余咬合齿数
  决定实际拉程（咬合齿数 × 每齿线距 = 满行程 ÷ 扇齿齿数）——贯通
  timing（行程/行程比）/ motor（弹簧反射扭矩）/ dynamics / ballistics 全链路
- ⚠实测方向（已入测试）：13.5 齿天梯 → 行程 51.05mm、储能/初速下降
  （3479→2786 mJ、70.8→62.6 m/s）、曲线模式电机负载转速升高（拉程短
  反射扭矩小）；但**撞击剩余速度略升**（4.32 vs 4.29 m/s，拉程短气垫缓冲
  吸收更少）→ 回位稳定时间略增（2.202 vs 2.185 ms）——回弹时间的影响
  方向由联动积分决定，勿假设单调
- `Timeline.remain_teeth` int → float（半齿），啮合齿数判定/报告改 %g；
  报告「切齿方案」行显示 天梯齿数与咬合上限；网页器件参数区新增
  天梯齿数 + 活塞质量g 输入（/api/default 自动预填，零硬编码）
- 新增回归测试 15 项（第 19 节；引擎 167 项）+ 接口 15 项全部通过；
  版本号 0.6.8；基线 v3.1 + 变更记录同步

## v0.6.7 变更（默认参数审查：文档同步 + 预压派生 + RPM 上限）
- ✅默认参数审查（用户要求：检查默认参数合理性）结论：参数本体自洽合理
  （实测/确认值与基线、default.json、网页预填逐项一致），但发现文档失同步
  与 3 个参数设计问题，本批次修复：
- ①文档同步：README 3 处旧供蛋速率（高级 50/压力 60 → 60/80 发/秒，
  v0.6.5 遗漏）+ 基线版本引用 v2.2→v3.0 + 测试数量修正；需求基线 FR-03
  第 6 项压气指数措辞修正为 min(行程比, 气缸系数)（v2.4 水桶效应后遗漏）
- ②弹簧预压一致性：`弹簧预压mm` 未显式覆盖时由 load_config 按
  「自由长度 − 装配长度」自动派生（显式覆盖优先；派生 ≤0 报中文错误）——
  修复只覆盖长度参数后预压与报告显示互相矛盾的问题；`弹簧自由/装配/满压
  长度` 在 README/components 注释标注为展示项（满压长度不参与计算）
- ③`电机.标称转速RPM` 增加上限校验（RPM_MAX=100000，与网页输入框一致）
- 新增回归测试 6 项（第 18 节；引擎 152 项）+ 接口 15 项全部通过；
  版本号 0.6.7；基线 v3.0 + 变更记录同步
- 审查中识别但**未改**的项（低优先，待用户决定）：电机负载 80%/电池跌落
  10%/25%/供蛋速率 90% 等判定线硬编码在 diagnosis.py 未入 Thresholds 表；
  `velocity_warn_ms` 字段名后缀 ms 实为 m/s；前端滑条范围窄于校验范围（UX）

## v0.6.6 变更（用户需求：界面显示默认值）
- ✅网页「判定阈值」「器件参数覆盖」输入框不再留空，预填系统默认值供
  参考或修改；清空某项 = 恢复该系统默认
- 默认值单一数据源：后端 `/api/default` 从引擎器件表（DeviceParams /
  Thresholds）按中文键映射（DEVICE_KEY_MAP / THRESHOLD_KEY_MAP）统一导出
  标量参数（25 项器件 + 6 项阈值；表类型如 弹簧刚度表/气缸系数表/
  最大供蛋速率表 不下发）——前端零硬编码，改默认值无需动网页
- 导入配置缺失项回退显示默认值（DEVDEF/THDEF 前端缓存）；导出 JSON 将
  包含与默认相同的显式覆盖（合法且结果一致）
- 新增接口测试 1 项（接口 15 项）+ 引擎 146 项全部通过；版本号 0.6.6；
  基线 FR-07 + 变更记录 v2.9 同步

## v0.6.5 变更（用户更正：供蛋方式速率）
- ✅高级波轮最高供弹速率 50 → **60 发/秒**；压力弹匣 60 → **80 发/秒**
  （✅用户确认：名称保留「压力弹匣」不变）
- 最小供蛋间隔 = 1000÷上限：高级波轮 16.7ms、压力弹匣 12.5ms（普通波轮 30 不变）
- 同步：网页下拉（≤60/≤80 发/秒）、rear_cut_heavy.json 示例、判定建议文案、
  README、需求基线 FR-02/FR-03 + 变更记录 v2.8；版本号 0.6.5
- 测试：改写 2 处断言 + 新增第 17 节回归 3 项（高级波轮 60 / 压力弹匣 80 上限
  与间隔、「压力」旧名映射）（引擎 146 项）+ 接口 14 项全部通过

## v0.6.4 变更（用户确认：选型后负载系数与标称转速锁定不可修改）
- ✅曲线模式（选 电机.型号）下：**负载系数固定为 1**、**标称转速RPM 锁定为
  该电机曲线空载转速**，两项均不允许修改——
  ①JSON 校验（params.load_config）：曲线模式显式配置 负载系数 → 中文报错；
  标称转速RPM 可省略/填 0（自动按曲线值填入），显式填写须与曲线值一致
  否则报错；②引擎（motor.py/timing.py）：负载转速与空转弧段转速不再乘
  负载系数（原无刷 0.9/有刷 0.8 降额默认移除）；③报告：移除「已乘负载系数」
  标注（曲线模式）；④固定转速模式（未选型号）行为完全不变（默认 0.8 可覆盖）
- 网页（index.html）：电机下拉选项带 data-rpm 标称值；选型号后 标称转速
  输入框（含滑条）自动同步显示该电机标称值并**禁用**，负载系数清空并禁用；
  切回固定转速模式恢复可编辑；曲线模式 payload 只下发 型号（不下发转速/系数）
- 示例配置 motor_4w8.json 移除冗余 标称转速RPM（=曲线值）；README/需求基线
  FR-01.6+FR-02+假设/变更记录 v2.7 同步；版本号 0.6.4
- 测试：改写 5 处受影响断言 + 新增第 16 节回归 3 项（显式负载系数报错、
  RPM 不符报错、RPM 等于曲线值通过），引擎 142 项 + 接口 14 项全部通过

## v0.6.3 变更（新增两款电机性能曲线）
- ✅电机.型号扩充为 4 款：新增 **超力无刷3W9**（空载 39000 RPM / 堵转
  401.80 mN·m / 空载 2.4A / 堵转 156A）与 **超力有刷3W3**（空载 32800 RPM /
  堵转 450.20 mN·m / 空载 2.8A / 堵转 145A），曲线来自「电机性能曲线/」
  下 CHAOLI 11.1V 官方性能图
- 空载转速口径：取图中 At No Load **表格精确值**（与既有 3W5=35500 一致；
  3W3 表格 32800 / 标题 33000，取表格值）
- 负载系数默认沿用类型口径（无刷 0.9 / 有刷 0.8）；电池模型/堵转判定/
  变转速循环对新型号自动生效，无需改动
- 同步：网页电机下拉 4 项、示例配置新增 configs/examples/motor_3w9.json
  与 motor_3w3.json（曲线模式省略标称RPM）、README 电机章节、需求基线
  FR-02 + 变更记录 v2.6；版本号 0.6.3
- 新增回归测试 11 项（第 15 节）：型号注册、3W9/3W3 负载转速精确值
  （16:1/M90/负载系数1.0 下 32282 / 27757 RPM）、峰值扭矩占比、类型默认
  负载系数、3W3 RPM 省略、负载转速排序 4W8 > 3W9 > 3W3、双型号全流程冒烟

## v0.6.2 变更（用户模型更正：电池满电 4.2V/芯）
- ✅用户更正：电机性能曲线基于**标称 3.7V/芯**（11.1V = 3S 标称），
  实际工况电芯均**充满至 4.2V/芯**——配置电池后按满电电压 4.2V×S 起算
  （3S 满电 12.6V，较曲线测试电压 11.1V 高 ~13.5%），负载转速/堵转扭矩
  同步缩放（如 3S 满电 4W8 负载 47473 RPM vs 曲线电压 40987）
- 电压跌落**自满电起算**：v_eff = 4.2V×S × sag（sag = 限流÷峰值需求）；
  峰值电流需求与电压无关（I=T/Kt）→ sag 比率不变 → 电池判定级别不变
- BatteryInfo 新增 v_start（满电起始电压），v_nom 保留标称 3.7V×S 作规格展示；
  报告电池描述附满电电压；未配置电池仍按曲线测试电压 11.1V 理想电源计算
  （文案注明实际满电 3S ≈ 12.6V）；网页电池下拉标注满电电压
- 新增回归测试 4 项（引擎 127 项）+ 接口 14 项全部通过

## v0.6.1 最近变更（用户模型更正：气缸有效容积水桶效应）
- ✅用户更正：气缸开孔位置**固定于缸体**（以满行程标定），不随切齿移动——
  有效密封段 = **min(气缸系数×满行程, 实际行程)**（水桶效应），
  替换原「气缸系数×当前行程」的叠加缩减算法：
  ①行程仍覆盖开孔段（如 50%缸+后切2）→ 密封容积**不随切齿缩减**
  （验证：有效排量 13.46 = 无切 13.46 cm³）；
  ②行程短于开孔段（如 50%缸+前后各切6）→ 活塞头始终在孔位前方 →
  **全程密封、无自由泄压段**（验证：有效排量 = 全行程排量 6.73 cm³）
- 压气指数 = min(行程比, 气缸系数)（原为行程比×气缸系数）
- ⚠连带物理推论（已入测试）：高百分比缸孔位偏后（70%缸孔距前端 42.35mm），
  重后切时活塞越孔极小（70%缸+后切4 仅 3mm）→ 开孔缓冲显著减小 →
  气密判定转危险；50%缸孔位在中点（30.25mm）后切4 仍有 ~15mm 泄压段 → 放宽
- 影响：切齿配置的初速/缸压普遍比旧模型**更高**（旧模型低估密封容积）；
  无切配置行为完全不变（锚点 67.3 m/s 保持）
- 测试按修正后物理重写第 5/6 节 + 新增水桶验证，引擎 123 项 + 接口 14 项

## v0.6.0 最近变更（用户模型更正：满压长度派生 + 天梯复位模型）
- 主弹簧满压长度改按「装配长 − 实际行程」派生：默认 42.0（=102.5−60.5，
  ✅用户更正：原独立实测 40~45 可能有误）；切齿后实际拉满长度 =
  装配长 − 行程（如后切4：102.5−45.375=57.125），报告新增「弹簧长度」行
- 天梯复位（回位稳定）由经验公式 0.5ms/(m/s) 改为**弹簧-活塞简谐回位模型**
  （ballistics.settle_time_ms）：回弹速度 v_r = 回弹系数×撞击速度；
  复位时间 t = 2·atan(v_r/(ω·x0))/ω（ω=√(k/m)，x0=撞击时弹簧压缩量=预压）
  —— 刚度/压缩长度越大复位越快，撞击越重复位越久；保留 2ms 下限
- 新增器件参数「撞击回弹系数」piston_head_restitution（默认 0.5 估算，可覆盖）
- ⚠模型洞察：整枪对比时刚度更大的弹簧撞得也更重（M110 撞击 ≈7.1 vs
  M75 ≈3.3 m/s），回位优势被更猛回弹部分抵消——「刚度→复位快」应在
  同撞击条件下成立（函数 docstring 与测试均已注明）
- 报告新增「回位稳定」行（撞击/回弹速度 + 复位时间）；版本 0.6.0；
  需求基线新增 FR-01.9；模型假设第 6 条顺带修正（负载系数口径）
- 测试新增 7 项（引擎 118 项）+ 接口 14 项全部通过

## v0.5.9 最近变更（代码审查修复批次，2026-09-08）
- 修复：循环事件表「推嘴开始缩回」时刻原硬编码 0.0（v0.5.4 角度锚定 11.25°
  后失准）→ Timeline 新增 `retract_start_ms`（弧段换算），报告/网页共用修正
- 修复：「内管管径」代码默认 7.3 → **7.5**（与 default.json/网页/基线 FR-02
  统一；省略键与显式默认路径行为一致）
- 修复：曲线模式（选 电机.型号）下 `标称转速RPM` 改为可省略/可填 0
  （缺省按曲线空载转速填入，不参与计算）；固定转速模式仍必填
- 修复：结论建议中延时器毫秒改用 `tl.delay_ms`（弧段精确值；原用
  周期×占孔比近似，变转速下两值不一致）
- 修复：气密哨兵统一——正常路径无有效压气时 `p_rise_ms=None`（原为 inf），
  与退化路径 `_degenerate` 一致；气密裕量哨兵 999
- 校验：切齿齿数拒绝布尔值；`ballistics.strike_ms` 注释更正（相对扇齿 0°
  基准）；电池续航注释标注保守估算口径（匀速周期+全周期拉簧平均电流）
- webapp：`simulate_payload` 改用已算结果调 `report.render` 生成报告文本
  （原 `render_full` 全流程重跑，单次模拟 67→37ms）
- 版本号 `__init__.py` 0.1.0 → 0.5.9；git 仓库初始化（Windows git：
  `C:\Program Files\Git\cmd\git.exe`；PATH 上的 git 是 cygwin 版勿用）
- 测试新增回归 9 项：事件表时刻×2、默认管径、曲线模式 RPM 可省×2、
  退化路径哨兵×3、切齿布尔值校验

## 版本沿革（新→旧摘要；详见 需求文档/需求基线.md 变更记录）
- v0.5.8 输入默认值调整：内管 210mm / 13:1 / 50%（解析+default.json+网页同步）
- v0.5.7 变转速循环：拉动弧按负载转速、空转弧按近空载转速分段换算；
  延时器角度级推移；有效气容积由联动积分覆盖（管长饱和 ≈71.4 m/s）
- v0.5.6 负载系数默认按电机类型（无刷 0.9 / 有刷 0.8 / 固定 0.8）
- v0.5.5 拉桥旗 S 形槽形语义（切旗 = 切缓冲尾，需强拉桥簧）
- v0.5.4 拉桥柱几何：推嘴缩回起点 = 拾取+1 齿距（11.25°）；延时器 10 孔/圈
- v0.5.3 主弹簧实测：自由 160~175/装配 100~105/满压 40~45 → 预压 62.5mm；
  刚度表 M75:0.45…M110:0.85；气动效率 0.21
- v0.5.2 改装件参数化（安装延时器 / 切拉桥旗 布尔开关）
- v0.5.1 电池模型（sag/续航）+「电池放电」判定
- v0.5.0 电机曲线（超力无刷4W8 / 有刷3W5）+「电机负载」判定
- v0.4.x 弹道重写为气垫-弹丸联动积分（2μs）；参数审查批次；水弹 0.20g
- v0.3.x 扇齿 16 齿×11.25°=180°；二号波通用基准；网页界面

## 下一步（等用户输入）
1. P-2 剩余校准：活塞质量、余隙容积实测；气动效率用实测初速反标精修
2. P-4 批量多配置对比（优先级待定）
3. P-5 波轮供蛋速率细化方式（待定）

## 用户偏好
- 全中文交流与输出；JSON 配置用中文键
- 术语：前切=推迟活塞拾取、后切=提前释放（用户已确认）
- 波箱：通用二号波
- 供蛋方式按最高供弹速率区分（✅v0.6.5 更正）：普通波轮 30 / 高级波轮 60 /
  压力弹匣 80 发/秒