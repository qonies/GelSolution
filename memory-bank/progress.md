# 当前进度 (Progress)

## 已完成（v0.8.0，需求基线 v3.6）
- [x] **v0.8.0 网页参数分区与预设（✅用户需求）**：左面板重组 6 分区
      （电机/齿轮/电池/气缸/弹簧/其他），5 区预设下拉（configs/presets/
      <分区>/<名称>.json，内置 22 套，GET /api/presets 下发）；选预设整区
      载入、手动改自动转自定义；顺带修复 8 个器件输入缺实时重算监听；
      引擎 189 项 + 接口 21 项全部通过
- [x] **推送前审查批次（文档同步）**：README 残留旧值修正（基线引用 v3.5、
      引擎测试数 189 两处、气动效率 0.243 两处）+ 移除弃用「最小供蛋时间ms/
      供蛋最小间隔」残留行；report「气缸规格」长度 %g 显示（72.5 不再截为 72）；
      引擎 189 项 + 接口 15 项全部通过；已推送 GitHub（origin/master）
- [x] **品牌引用清理（✅用户要求）**：全文移除第三方品牌字样（14 处：
      代码注释/报告文案/网页/README/需求基线/记忆库 → 「通用二号波/实测」
      中性表述）；引擎 189 项 + 接口 15 项全部通过
- [x] **校准指南文档（✅用户需求）**：新增 docs/校准指南.md（实测校准操作
      手册：参数总览/工具清单/分优先级方法/反标公式/填写示例/记录表模板）；
      README 文档章节、自校准流程、目录结构同步链接
- [x] **v0.7.2 单循环时序终点更正（✅用户指出）**：终点=下一循环首个事件
      （前切≥2 时为下一循环推嘴缩回，早于下一循环拾取）；Timeline 新增
      next_retract_ms；引擎 189 项 + 接口 15 项全部通过
- [x] **v0.7.1 供蛋判定语义澄清（✅用户澄清）**：供蛋能力上限=实际支持发射
      的能力，直接与射速比较；移除「供蛋窗口 vs 1000÷上限」判定（11→10 项）；
      供蛋窗口保留为时序信息；最小供蛋时间ms 弃用（键接受忽略）；延时器
      结论改定性；引擎 184 项 + 接口 15 项全部通过
- [x] **v0.7.0 开孔段漏气模型更正（✅用户模型更正）**：4 横槽有漏非全漏 →
      新增「开孔段保留系数」（0.6 估算，γ×保留系数等效多变），密封段自盖孔
      保留压力起算；气动效率重校准 0.21→0.243（70缸锚点 ≈71 保持）；
      默认初速 67.3→58.9；重后切气密收紧；引擎 180 项 + 接口 15 项全部通过
- [x] **v0.6.9 天梯齿数口径更正（✅用户更正）**：默认 13.5（原装）；半齿=
      磨低第二齿仍整齿咬合 → 计整齿（13.5→14、11.5→12）；拉程 = 满行程 ×
      min(剩余咬合, 14)/14（原装咬合 14 ↔ 满行程 60.5，默认行为/锚点不变，
      超过封顶）；引擎 169 项 + 接口 15 项全部通过
- [x] **v0.6.8 新增「天梯齿数」参数（✅用户需求）**：咬合上限
      = min(扇齿, 天梯)，拉程 = 咬合齿数 × 每齿线距，贯通全链路
      （行程/储能/初速/电机反射/回位稳定）；允许 13.5/11.5 等半齿；
      网页/报告/判定同步；新增回归 15 项（引擎 167 项）+ 接口 15 项全部通过
- [x] **v0.6.7 默认参数审查修复批次**：文档同步（README 供蛋速率 60/80 三处、
      基线版本/测试数；需求基线 FR-03 压气指数 min 口径）；弹簧预压未显式
      覆盖时按「自由−装配」自动派生（显式覆盖优先，≤0 报错），弹簧长度参数
      标注展示项；标称转速RPM 上限 100000 校验；新增回归 6 项
      （引擎 152 项）+ 接口 15 项全部通过
- [x] **v0.6.6 界面预填默认值（✅用户需求）**：网页「判定阈值」「器件参数
      覆盖」输入框预填系统默认值（由 /api/default 从引擎器件表统一导出，
      单一数据源）；清空 = 恢复默认；导入缺失项回退默认显示；新增接口
      测试 1 项（接口 15 项）+ 引擎 146 项全部通过
- [x] **v0.6.5 供蛋方式更正（✅用户）**：高级波轮 50→**60 发/秒**、
      压力弹匣 60→**80 发/秒**（名称保留「压力弹匣」不变）；网页/示例配置/
      判定文案/README/基线 v2.8 同步；新增回归 4 项
      （引擎 146 项）+ 接口 14 项全部通过
- [x] **v0.6.4 曲线模式锁定（✅用户确认）**：选 电机.型号 后 负载系数固定 1、
      标称转速RPM 锁定为该电机曲线空载转速，两项均不可修改（JSON 显式配置
      报中文错误；网页输入框禁用并同步显示曲线值）；引擎负载/空转转速不再乘
      负载系数（移除 0.9/0.8 降额默认）；固定转速模式不变；新增回归 3 项、
      改写 5 处断言（引擎 142 项）+ 接口 14 项全部通过
- [x] **v0.6.3 新增电机性能曲线两款**：超力无刷3W9（39000 RPM / 401.80 mN·m /
      156A）与 超力有刷3W3（32800 RPM / 450.20 mN·m / 145A，空载取表格值口径）；
      MOTOR_CURVES 4 款、网页下拉/示例配置/README/需求基线 v2.6 同步；
      新增回归测试 11 项（引擎 138 项）+ 接口 14 项全部通过
- [x] 需求澄清与技术路线确定（Python + JSON + CLI 中文报告 + 本地网页）
- [x] 引擎全流程：motor / timing / dynamics / ballistics / feeding / diagnosis / report / cli / webapp
- [x] 11 项判定：电机负载、电池放电、打齿、气密时序、供蛋速率、供蛋窗口、啮合齿数、压气匹配、初速、气量管长匹配、出膛时序
- [x] 电机曲线接入（超力无刷4W8 / 有刷3W5）、电池模型（sag 按限流/峰值需求缩放）
- [x] 弹道层：气垫-弹丸联动积分模型（2μs 步长，能量守恒已验证）
- [x] 改装件参数化（延时器 / 切拉桥旗，带回位完成下限保护）
- [x] 变转速循环（v0.5.7）；负载系数默认按电机类型（v0.5.6）
- [x] 主弹簧实测参数（v0.5.3）；输入默认值调整（v0.5.8：210mm/13:1/50%）
- [x] 本地网页界面（实时调参）；切齿方案枚举对比 + 建议；参数置信度标注
- [x] **v0.6.2 用户模型更正（电池满电工况）**：配置电池后按满电 4.2V/芯
      起算（3S 满电 12.6V，曲线为标称 3.7V/芯/11.1V），跌落自满电起算；
      BatteryInfo 新增 v_start；网页下拉标注满电电压
- [x] **v0.6.1 用户模型更正（水桶效应）**：气缸孔位固定于缸体（满行程标定），
      有效密封段 = min(气缸系数×满行程, 实际行程)，替换叠加缩减算法；
      压气指数 = min(行程比, 气缸系数)；高百分比缸+重后切开孔缓冲减小
      （气密判定随之变化）；测试重写第 5/6 节 + 新增水桶验证
- [x] **v0.6.0 用户模型更正**：主弹簧满压长度按「装配长 − 实际行程」派生
      （默认 42.0，原独立实测有误）；天梯复位改弹簧-活塞简谐模型
      （settle_time_ms：刚度/预压/回弹系数决定复位速度），新增器件参数
      「撞击回弹系数」（0.5 估算）；报告新增 弹簧长度/回位稳定 行
- [x] **v0.5.9 缺陷修复批次**：事件表推嘴缩回时刻、管径默认统一 7.5、
      曲线模式 RPM 可省、结论延时用弧段精确值、气密哨兵统一、布尔校验、
      webapp 去重复计算（67→37ms）、git 初始化、全套文档同步
- [x] 测试：引擎 127 项 + 网页接口 14 项全部通过

## 待办
- [ ] P-2 剩余校准：活塞质量、余隙容积实测；气动效率用实测初速反标精修
- [ ] P-4：批量多配置对比模式（优先级待定）
- [ ] P-5：波轮供蛋速率细化方式（待定）

## 已知问题
- 撞击回弹系数（0.5）为估算值，待慢录像实测校准；整枪对比时硬簧撞得更重，
  「刚度→复位快」在同撞击条件下成立
- 电池续航估算偏保守（匀速周期+全周期拉簧平均电流口径，代码注释已标注）
- 气缸缸型-管长匹配分析（✅v0.7.2 澄清）：同一内管下初速随缸型有峰值，
  短管最优缸型偏小、长管偏大（210管≈50%、350管≈60%、500~700管≈80%），
  大缸+短管因弹丸提前出膛浪费气量——**趋势为匹配物理而非缺陷**，
  数值幅度待实测验证（弹丸无静摩擦、提前蠕动可能轻微放大短管下大缸劣势）
- 弹丸受力面积用管径面积 + 无启弹摩擦，由气动效率校准吸收
  （换管径/弹径组合时校准误差会略变）